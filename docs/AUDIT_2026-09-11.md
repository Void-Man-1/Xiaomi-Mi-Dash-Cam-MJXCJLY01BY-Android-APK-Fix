# Repository audit — 2026-09-11

Status: pre-merge audit complete; only merge, production deployment verification, and stale-branch cleanup remain.

## Executive result

The repository's release/build evidence, source-kit invariants, automation, documentation, public metadata, security posture, GitHub Pages presentation, localization, and repository hygiene were audited against the published `v2.0.0` baseline.

No defect was found in the immutable release artifact itself. The audit found several repository/presentation maintenance defects, all of which have been corrected on the audit branch except stale branch deletion, which is deliberately deferred until after this audit PR is merged. The published `v2.0.0` APK was not replaced, re-signed, retagged, or modified.

## Immutable release baseline

- Release tag: `v2.0.0`
- Release asset: `Mi-Dash-Cam-2.0.0.apk`
- Asset size: `30,437,934` bytes
- Published SHA-256: `2F189C0D3A6C9965036EDDBFA927EB7FD720C47D611991EB5EC1D1055E89B887`
- Package: `com.banyac.mijia.app.eu`
- versionName: `2.0.0`
- versionCode: `20000`
- Signing certificate SHA-256: `BE7E580BAE6723900DD30952B1DF215B282B445BCADAFC621C03B8D9CF81A1BD`
- Original stock EU APK SHA-256 expected by the source kit: `3AC4C02EF5D43A0F9636637B1359073818C6799F3BFE3A7E2A38633F653D95A8`

The release digest agrees with `checksums/SHA256SUMS.txt`. The source-kit manifest and `source-kit/checksums/original-apk.sha256` agree on the stock input digest. The final pre-merge release re-check again returned the same release tag, asset size, and GitHub-reported SHA-256 digest.

## Scope

The audit covered:

- published GitHub Release metadata and exact APK identity;
- source-kit PowerShell parsing, path-containment behavior, patch payload hashes, manifest/checksum consistency, and release verification;
- release-link synchronization behavior, including current-version idempotence and a simulated later release;
- repository documentation, issue templates, public issue state, and publishing guidance;
- GitHub Actions syntax/linting and runtime maintenance;
- repository history secret scanning;
- GitHub Pages HTML/CSS/JavaScript, five locales, media delivery, social metadata, responsive behavior, video playback metadata, and language selector invariants;
- public navigational links;
- live repository description/topics versus the documented listing;
- stale branch inventory and merged-PR provenance.

## Findings ledger

| ID | Severity | Area | Finding | Resolution/status |
| --- | --- | --- | --- | --- |
| F-001 | Medium | Website/social metadata | `og:image` and `twitter:image` used a portrait application screenshot instead of a purpose-built share card. | Fixed: added a dedicated `1200×630` `site/social-preview.png` and synchronized all five locale metadata blocks. Production URL requires post-merge deployment verification. |
| F-002 | Medium | Public issue state | Issue #1 still described `v2.0.0` as a release candidate and said physical-camera acceptance was outstanding. | Fixed: issue #1 now identifies 2.0.0 as final and accurately states the completed Poco F6 / Android 16 physical-camera acceptance boundary. |
| F-003 | Low | Repository hygiene | Nine superseded feature/migration branches remain after their PRs were merged. | Verified cleanup candidates. Deletion is intentionally deferred until the audit PR is merged and production checks pass. |
| F-004 | Medium | GitHub Pages reliability | Repository-owned app icon and screenshots were loaded at runtime from `raw.githubusercontent.com`, creating an unnecessary cross-host dependency. | Fixed: Pages deployment now stages the icon, screenshots, evidence video, and poster into same-origin `site/media/` paths; all locale HTML uses those paths. |
| F-005 | Low | Automation maintenance | Two production workflows still used `actions/checkout@v4`, which emitted the deprecated Node runtime warning; the release-sync shell also triggered ShellCheck `SC2129`. | Fixed: production checkout usage moved to `actions/checkout@v6`; repeated redirects were grouped without changing updater behavior. |
| F-006 | Low | Publishing documentation | `docs/PUBLISHING_CHECKLIST.md` still instructed maintainers to leave GitHub Pages disabled although Pages is now an intentional deployed surface. | Fixed: checklist now documents the Actions-based Pages deployment and five-locales synchronization requirement. |
| F-007 | Informational | Security scanning | Gitleaks reported the APK signing-certificate SHA-256 fingerprint as a generic high-entropy key candidate. | Classified as a false positive after tracing the exact value. A public certificate fingerprint is verification metadata, not signing material or a reusable credential. No removal was made. |
| F-008 | Low | Repository metadata documentation | `docs/GITHUB_LISTING.md` had an older About description and an outdated topic set compared with the live repository. | Fixed: the document now matches the live About description and the actual 20-topic GitHub set and records the 20-topic limit. |

## Release and source-kit verification

The published release was downloaded independently during the audit rather than trusted from repository prose. Verification established the expected digest, package/version identity, APK signatures, certificate identity, ZIP/ELF alignment, and acceptance by the repository's own `source-kit/scripts/verify.ps1` logic.

The PowerShell source-kit scripts parse under both Windows PowerShell 5.1 and PowerShell 7. Path-containment tests rejected hostile escape cases. The stored patch payload hashes agree with the manifest. The source kit cannot perform a from-zero reconstruction using only files committed to Git because Xiaomi's original APK and selected vendor/original inputs are intentionally excluded; that is an explicit redistribution/licensing boundary documented by the project, not evidence of a broken verifier.

## Release synchronization verification

The release-sync workflow was exercised in a temporary audit workspace:

- running against the current `v2.0.0` state was idempotent;
- a simulated `v2.1.0` update changed all five GitHub Pages locales and all translated README download/version references expected by the workflow;
- the simulation did not alter the real published release.

## Website/browser verification

A staged Pages artifact was exercised in actual Google Chrome across all five locales (`en`, `pl`, `uk`, `de`, `ru`) at six viewport sizes: `360×800`, `390×844`, `768×1024`, `1024×900`, `1366×900`, and `1440×1000` — 30 locale/viewport cases in total.

The final browser pass reported `BROWSER_AUDIT_OK cases=30` and checked:

- no horizontal page or language-row overflow;
- language order `en`, `pl`, `uk`, `de`, `ru`;
- four rendered flag images with Russian text-only and Russian last;
- centered release/trust strip;
- all repository-owned page images loading from the staged site;
- three screenshot cards using uncropped `object-fit: contain` rendering;
- one playable evidence-video element per locale with metadata `576×1280` and duration above 260 seconds;
- evidence video/poster using same-origin `/media/` paths;
- the social-preview asset resolving locally at exactly `1200×630`;
- durable public-naming and repository-integrity validators passing after the browser assertions.

The audit harness was corrected during execution for two test-infrastructure mistakes (GitHub Actions shell invocation/module resolution) and one expected lazy-loading behavior. Those were not counted as product defects.

## Security and history review

The history-wide Gitleaks pass scanned 144 commits and found one candidate. Manual classification traced it to the intentionally published APK signing-certificate SHA-256 fingerprint. No private signing key, access token, password, API credential, or other reusable secret was identified by that scan.

The project continues to document that signing material, original vendor APKs, account/device data, unredacted captures, and credentials must not be committed.

## Public link review

A focused external-link pass checked 27 navigational links referenced by the README/docs and Pages `href`/`src` attributes. It found zero definitive `404`/`410` failures. Two third-party catalog links (APKFab and AppBrain) returned HTTP `403` to the GitHub runner and were retained as soft-review results rather than misclassified as broken URLs.

The new production `social-preview.png` URL was intentionally excluded from the pre-merge network pass because that file cannot exist on the live Pages deployment until this PR is merged. Its repository existence, metadata references, and exact dimensions are already validated; the deployed URL is a required post-merge check.

## Repository listing review

The live repository About description and 20 topics were treated as authoritative because they are newer and more complete than the old listing document. `docs/GITHUB_LISTING.md` was synchronized to that live state rather than rolling the repository metadata backward.

## Branch hygiene

At audit time the repository contains `main`, the active audit branch, and nine superseded branch names. The nine cleanup candidates all correspond to merged work:

- `migration/uniform-naming` — merged PR #2;
- `migration/post-rename-urls` — merged PR #3;
- `redesign-github-pages` — merged PR #4;
- `fix-site-language-validation` — merged PR #5;
- `fix-center-trust-strip` — merged PR #6;
- `fix-language-flags` — merged PR #7;
- `move-russian-language-last` — merged PR #8;
- `audit-layout-and-localization` — merged PR #9;
- `fix-proof-gallery-video` — merged PR #10.

Because several were squash-merged and therefore remain graph-diverged from current `main`, raw commit ancestry alone is not a safe deletion test. Merged-PR provenance is the relevant evidence. They should be deleted only after PR #11 is merged and the post-merge Pages/release checks succeed. The audit branch itself can then also be deleted.

## Durable regression protection added

`tools/verify_repository_integrity.py` is now part of the permanent `Verify public naming` workflow. It protects the current repository invariants, including:

- immutable 2.0.0 checksum references;
- five expected Pages locales and localized navigation;
- Russian selector last with no Russian flag image;
- social-card metadata and local asset presence;
- sitemap/localized URL relationships;
- staged same-origin repository media rather than reintroducing Raw GitHub runtime dependencies;
- release/site metadata relationships covered by the existing public-naming validator.

## Deferred/non-defect boundaries

The following are deliberately not represented as fixed by this audit:

- legal/redistribution review and project-owned licensing decisions remain unchecked in `docs/PUBLISHING_CHECKLIST.md`;
- secure offline backup/recovery of the 2.0.0 signing key remains an operational task outside the repository and is still unchecked;
- camera firmware OTA remains outside the completed physical-camera acceptance claim;
- exact from-original reconstruction still requires excluded original/vendor inputs supplied locally by the maintainer;
- the new social-card production URL must be checked after Pages redeploys from merged `main`.

## Pre-merge finalization results

The temporary audit workflows were removed. The workflow directory contains only the three intended production workflows. The clean branch then passed the permanent `Verify public naming` pull-request workflow, including `tools/verify_repository_integrity.py`.

The complete `main...audit/full-repository-2026-09-11` review shows the branch is a strict descendant of the current `main` base with no commits missing from `main`; it changes only the expected production workflows, audit/listing/publishing documentation, five localized Pages files, the new social card, the repository-integrity validator, and the audit design/plan records. No temporary `audit-*.yml` workflow remains in the final diff.

## Post-merge closeout gate

After merge, complete these final operational checks:

1. verify the deployed Pages home and all four localized routes return successfully;
2. verify the deployed `social-preview.png` returns successfully from the Pages origin;
3. verify the Pages deployment workflow completed successfully for the merged `main` commit;
4. re-check the release still reports the immutable v2.0.0 asset digest above;
5. delete the nine verified stale branches listed above plus the merged audit branch.

## Audit rule

A candidate was counted as a confirmed finding only when reproduced from repository state, workflow execution, release metadata, static analysis, browser behavior, or live GitHub configuration. Correct code was not rewritten for style alone, and scanner/network results were manually classified when an automated result could be a false positive or bot-block response.
